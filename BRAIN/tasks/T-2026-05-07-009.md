---
id: T-2026-05-07-009
title: Backport V3 reals din TRADE AI la Jarvis AI/server/
assignee: TBD (codex preferat)
status: proposed
priority: medium
estimateMin: 180
created: 2026-05-07T03:30:00Z
deps: [T-2026-05-07-003]
relates_to:
  - BRAIN/handoff/CLAUDE__to__codex__T-2026-05-07-003__ACCEPT.md
  - server/state/agentState.js (stub în Jarvis AI)
  - server/intent/router.js (stub în Jarvis AI)
  - server/risk/tiers.js (stub în Jarvis AI)
  - server/emergency/stopAll.js (stub în Jarvis AI)
  - server/audit/log.js (stub în Jarvis AI — parțial real)
---

## Context
T-003 a wirat `jervis-boot.mjs` la modulele V3 din `Jarvis AI/server/*`. Codex a semnalat corect: exporturile sunt reale, dar corpurile sunt stub-uri minimal-functional. Implementarea completă trăiește în `/Antigraity/TRADE AI/server/`:
- FSM `agentState.js` 192 LoC + 13 tests
- IntentRouter `router.js` 228 LoC + 18 tests
- RiskTiers `tiers.js` 143 LoC + 10 tests
- EmergencyStop `stopAll.js` 115 LoC + 7 tests
- AuditLog `log.js` 260 LoC + 12 tests (parțial deja în Jarvis AI)

## Goal
Backport implementările complete + tests în `Jarvis AI/server/` păstrând semnătura exporturilor (compat cu `jervis-boot.mjs` deja wired). Zero diff la `jervis-boot.mjs`.

## Done când
- [ ] `Jarvis AI/server/state/agentState.js` = real (192 LoC), exporturi compatibile.
- [ ] `Jarvis AI/server/intent/router.js` = real (228 LoC).
- [ ] `Jarvis AI/server/risk/tiers.js` = real (143 LoC).
- [ ] `Jarvis AI/server/emergency/stopAll.js` = real (115 LoC).
- [ ] `Jarvis AI/server/audit/log.js` = real complet (260 LoC) — replace parțial.
- [ ] `Jarvis AI/tests/` adaugă cele 60 tests corespunzătoare (13+18+10+7+12).
- [ ] `node --test tests/` → ≥150/150 (90 existente + 60 backportate).
- [ ] Smoke `node jervis-boot.mjs` rămâne valid (zero regresie).
- [ ] `jervis-boot.mjs` neatins (`git diff jervis-boot.mjs → empty`).

## Constrângeri
- NU modifica semnăturile exporturilor (jervis-boot.mjs deja le consumă).
- NU schimba paths import în `jervis-boot.mjs`.
- Tests originale TRADE AI au paths absolute la fixture-uri — adaptează la Jarvis AI.
- Folosește același format raport (VERDICT/DONE/TESTED/RISK/BROKEN/NEXT).

## Files atinse (preview)
- server/state/agentState.js (replace stub → real)
- server/intent/router.js (replace stub → real)
- server/risk/tiers.js (replace stub → real)
- server/emergency/stopAll.js (replace stub → real)
- server/audit/log.js (extend parțial → complet)
- tests/state.test.js (NEW)
- tests/intent.test.js (NEW)
- tests/risk.test.js (NEW)
- tests/emergency.test.js (NEW)
- tests/audit.test.js (extend dacă există)
- codex Jarvis ai/handoff/T-2026-05-07-009.md

## Notes
- Verificare critică post-backport: snapshot `/` la `:7777` să includă `fsm.history`, `audit.lastEvent`, `risk.lastTier`, `intent.lastDecision`.
- Operator confirmă acest task înainte de start.

